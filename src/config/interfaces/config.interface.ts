import { BaseConfigInterface } from "./base.config.interface";

/**
 * Application config interface
 * Consumers should extend this with their own application-specific config
 */
export interface ConfigInterface extends BaseConfigInterface {}

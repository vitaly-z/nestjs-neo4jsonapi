import { DataMeta } from "../../../common/interfaces/datamodel.interface";

export const tokenUsageMeta: DataMeta = {
  type: "tokenusages",
  endpoint: "tokenusages",
  nodeName: "tokenusage",
  labelName: "TokenUsage",
};
